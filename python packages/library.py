"""
library =>> 
			library contains several packages :
			(pack1, pack2, pack3...)

	package =>>
				each package contain several modules :
				(pack1 -> module1, module2, module3)
				(pack2 -> module1, module2, module3)
				(pack3 -> module1, module2, module3)
				..
				...
				....
		module =>>
				   each module contain several functions :
				   (module1 -> x(), xx(), xxx(), xxxx())
				   (module2 -> y(), yy(), yyy(), yyyy())
				   (module3 -> z(), zz(), zzz(), zzzz())
				   (module4 -> x(), xy(), xyz(), xxyy())
				   ..
				   ...
				   ....
				   

"""